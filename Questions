1. What is nav-tab ? where is that code starts and ends
2. how to make any tab as current tab
3. where is the header in the code show  with line number
4. can we add more menus in the tab
5. what is tab content - show in code starts
